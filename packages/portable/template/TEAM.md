# Workspace ownership

Principal: TODO
Active coordinator session: TODO
Coordinator ownership established at (UTC): TODO

Only the principal or the explicitly designated coordinator updates this file. Stop the old coordinator before transferring ownership to a new session.

| Participant ID | Role | Model/client | Verified read/write access | Budget/limits |
|---|---|---|---|---|
| TODO | coordinator | unknown | unverified | unconfigured |
| TODO | producer | unknown | unverified | unconfigured |
| TODO | reviewer | unknown | unverified | unconfigured |

## Permissions

Never send email. List any other workspace-wide boundaries here. Each task must separately state its authorized outcome, allowed actions, and exclusions. Filling in a role does not authorize external actions or spending.

## Handoffs

Notification method: TODO (for example, a person opens the next agent session).
Storage location and access method: TODO (local folder, approved shared storage, or manual transfer).

Verify access with a harmless sample before assigning real work. A chat-only participant may return file contents for the coordinator to save on its behalf; record both the author and the person saving it.
